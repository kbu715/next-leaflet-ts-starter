import { icon } from "leaflet";
const CarIcon = icon({
  iconUrl: "Car.svg",
  iconSize: [36, 36],
  iconAnchor: [18, 36],
});

const ArrowIcon = icon({
  iconUrl: "Arrow.svg",
  iconSize: [36, 36],
  iconAnchor: [18, 36],
});

export { CarIcon, ArrowIcon };
