import { icon } from "leaflet";
const CarIcon = icon({
  iconUrl: "Car.svg",
  iconSize: [24, 24],
  iconAnchor: [12, 24],
});

const ArrowIcon = icon({
  iconUrl: "Car.svg",
  iconSize: [24, 24],
  iconAnchor: [12, 24],
});

export { CarIcon, ArrowIcon };
